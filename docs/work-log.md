# Partner Portal Work Log

## 2026-05-30 월 필터 및 대표 요약 개선

작업 내용

1. 협력사/기사 포털에 월 필터를 추가했다.
2. 기존 getPartnerJobs 전체 월 조회 구조는 유지했다.
3. 전체 / 월별 탭 방식으로 프론트에서만 필터링하도록 구성했다.
4. 월 필터와 기존 상태 필터가 함께 동작하도록 변경했다.
5. partner 권한에는 대표 요약 영역을 추가했다.
6. partner 권한 요약에는 현장, 진행, 사진필요, 완료, 미배정, 지급시공비 합계를 표시한다.
7. engineer 권한 요약에는 현장, 진행, 사진필요, 완료만 표시한다.
8. engineer 권한에는 지급시공비, 계약금액, 차액, 차액률을 표시하지 않는다.

수정 파일

1. src/App.jsx
2. docs/work-log.md

검증

1. npm run build 성공
2. Apps Script 수정 없음
3. 시트 구조 수정 없음
4. API 조회 범위 변경 없음
5. 권한 로직 변경 없음

모바일 확인 포인트

1. 로그인 후 상단에 월 필터가 보이는지 확인한다.
2. 전체 / 26.04 / 26.05 / 26.06 탭이 가로 스크롤로 눌리는지 확인한다.
3. 월을 누르면 대표 요약 숫자와 목록 건수가 함께 바뀌는지 확인한다.
4. 상태 필터 전체/미배정/사진필요/진행중/완료가 월 필터와 같이 동작하는지 확인한다.
5. 협력사 계정에서는 지급시공비 합계가 보이는지 확인한다.
6. 기사 계정에서는 금액 정보가 보이지 않는지 확인한다.

## 2026-05-30 협력사/기사 포털 월 필터 확인 및 검증

작업 목적

1. 협력사/기사 포털에서 월별 현장 구분 가능 여부를 확인했다.
2. 전체 / 월별 필터 구조가 현재 전체 월 조회 방식과 충돌하지 않는지 검증했다.

확인 결과

1. `selectedMonth` 상태가 존재한다.
2. `monthOptions` 계산 로직이 존재한다.
3. `monthVisibleJobs` 월별 필터 로직이 존재한다.
4. `MonthFilter` 컴포넌트가 존재한다.
5. 협력사 권한과 기사 권한 모두에 월 필터가 적용된다.
6. 모바일에서 월 탭은 가로 스크롤 방식으로 동작한다.
7. 현재 테스트 데이터는 26.04만 존재한다.
8. 26.05 / 26.06 데이터가 추가되면 `jobs` 데이터에서 월 목록을 자동 추출해 탭에 표시할 수 있다.

검증 결과

1. 빌드 성공 상태를 확인했다.
2. API 수정 없음.
3. Apps Script 수정 없음.
4. 시트 수정 없음.
5. 추가 코드 수정 없음.

현재 상태

1. 월 필터 기능은 완료 처리 가능하다.
2. 실제 모바일 화면에서 협력사/기사 로그인 후 최종 확인만 남아 있다.

다음 작업

1. 모바일 UI 최종 마감.
2. LOG_HISTORY 검증.
3. 잠금 기능 검증.
4. 삭제요청 프로세스 검증.
